import React from "react";


const HeroSec = () => (
    <section className={"hero is-primary is-fullheight"}>
        <div className={"hero-body"}>
            <div className={"container"}>
                <h1 className={"has-text-centered is-size-1"}><span className={"wave"}>👋</span></h1>
                <h1 className={"has-text-centered is-size-2 has-text-weight-semibold"}>JUSTIN BERGHAHN</h1>
            </div>
        </div>
    </section>
);

export default HeroSec;
